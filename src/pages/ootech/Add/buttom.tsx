import { IconButton } from "@mui/material"
import { MdAdd } from "react-icons/md"

export const BtnAdd = () => {
    return (
        <IconButton sx={{ height: 44 }}>
            <MdAdd />
        </IconButton>
    )
}