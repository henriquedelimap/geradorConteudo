import { Stack, Typography } from "@mui/material"

export const CAlendariosPage = () => {
    return (
        <Stack direction='row' spacing={2} alignItems='center' justifyContent='center' sx={{ width: '100vw', height: '100vh' }} >
            <Typography fontFamily='Outfit' fontWeight={200} variant='h3'>calendários</Typography>
        </Stack>

    )
}